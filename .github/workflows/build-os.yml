name: Build DuyKhanhOS Live ISO

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-iso:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install ISO Generation Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y squashfs-tools xorriso grub-pc-bin mtools dosfstools

      - name: Extract System Core via Docker
        run: |
          mkdir -p chroot_dir
          docker create --name duykhanh_core ubuntu:noble
          docker export duykhanh_core | tar -C chroot_dir -xf -
          docker rm duykhanh_core

      - name: Force Static Package Repositories and Lock
        run: |
          # 1. Xóa sạch tận gốc thư mục cấu hình nguồn cũ để triệt tiêu link ubuntu.com lỗi
          sudo rm -rf chroot_dir/etc/apt/sources.list.d
          sudo mkdir -p chroot_dir/etc/apt/sources.list.d
          
          # 2. Ghi đè cứng danh sách kho lưu trữ chính thức Singapore không mã hóa
          sudo cat <<'EOF' | sudo tee chroot_dir/etc/apt/sources.list
          deb [trusted=yes] http://ubuntu.com noble main restricted universe multiverse
          deb [trusted=yes] http://ubuntu.com noble-updates main restricted universe multiverse
          deb [trusted=yes] http://ubuntu.com noble-security main restricted universe multiverse
          EOF

          # 3. Tạo file chặn (Block) ép hệ thống không được tự động sinh file cấu hình mạng ẩn
          sudo touch chroot_dir/etc/apt/sources.list.d/ubuntu.sources
          
          # 4. Sử dụng lệnh chattr +i để KHÓA CỨNG file cấu hình, không cho phép bất kỳ lệnh nào (kể cả dpkg) sửa đổi hoặc kích hoạt lại link lỗi
          sudo chattr +i chroot_dir/etc/apt/sources.list
          sudo chattr +i chroot_dir/etc/apt/sources.list.d/ubuntu.sources

      - name: Synchronize Network DNS
        run: |
          sudo cp /etc/resolv.conf chroot_dir/etc/resolv.conf

      - name: Fix Dev Null Permission and Mount System
        run: |
          sudo rm -f chroot_dir/dev/null
          sudo mknod -m 666 chroot_dir/dev/null c 1 3
          
          sudo mount --bind /dev chroot_dir/dev
          sudo mount --bind /proc chroot_dir/proc
          sudo mount --bind /sys chroot_dir/sys

      - name: Install Graphic Suite and Applications
        run: |
          export DEBIAN_FRONTEND=noninteractive
          
          # Kích hoạt kiến trúc 32-bit cho Steam
          sudo chroot chroot_dir dpkg --add-architecture i386
          
          # Cập nhật danh sách ứng dụng tiêu chuẩn từ file nguồn đã được khóa an toàn
          sudo chroot chroot_dir apt-get -o Acquire::https::Verify-Peer=false -o Acquire::AllowInsecureRepositories=true update
          
          # Tiến hành cài đặt toàn bộ hệ thống đồ họa, bộ gõ Unikey, Chromium và Steam từ kho lưu trữ thực tế
          sudo chroot chroot_dir apt-get install -y --allow-unauthenticated --no-install-recommends \
            linux-image-generic casper sudo systemd-sysv ca-certificates \
            xorg xfce4 xfce4-goodies lightdm plank \
            network-manager network-manager-gnome wpasupplicant chromium-browser \
            fcitx fcitx-unikey fcitx-config-gtk vlc libreoffice mousepad thunar \
            calamares calamares-settings-ubuntu-common calamares-settings-lubuntu gparted curl git \
            steam

      - name: Unmount System Filesystems
        if: always()
        run: |
          # Mở khóa file hệ thống để chuẩn bị cho quá trình nén SquashFS
          sudo chattr -i chroot_dir/etc/apt/sources.list || true
          sudo chattr -i chroot_dir/etc/apt/sources.list.d/ubuntu.sources || true
          
          sudo umount -l chroot_dir/dev || true
          sudo umount -l chroot_dir/proc || true
          sudo umount -l chroot_dir/sys || true

      - name: Configure Auto-Login and User Account
        run: |
          sudo mkdir -p chroot_dir/etc/lightdm/lightdm.conf.d
          sudo cat <<'EOF' | sudo tee chroot_dir/etc/lightdm/lightdm.conf.d/autologin.conf
          [Seat:*]
          autologin-user=duykhanh
          autologin-user-timeout=0
          user-session=xfce
          EOF

          sudo chroot chroot_dir useradd -m -s /bin/bash -g sudo duykhanh
          sudo chroot chroot_dir passwd -d duykhanh

          sudo mkdir -p chroot_dir/etc/sudoers.d
          sudo cat <<'EOF' | sudo tee chroot_dir/etc/sudoers.d/g_sudo
          %sudo   ALL=(ALL:ALL) NOPASSWD: ALL
          EOF
          sudo chmod 0440 chroot_dir/etc/sudoers.d/g_sudo

      - name: Setup System AutoStart Configuration
        run: |
          sudo mkdir -p chroot_dir/home/duykhanh/.config/autostart
          
          sudo cat <<'EOF' | sudo tee chroot_dir/home/duykhanh/.config/autostart/plank.desktop
          [Desktop Entry]
          Type=Application
          Exec=plank
          Name=Plank Dock
          X-GNOME-Autostart-enabled=true
          EOF

          sudo cat <<'EOF' | sudo tee chroot_dir/home/duykhanh/.config/autostart/fcitx.desktop
          [Desktop Entry]
          Type=Application
          Exec=fcitx -d
          Name=Fcitx Input Method
          X-GNOME-Autostart-enabled=true
          EOF

          sudo mkdir -p chroot_dir/etc/X11/Xsession.d
          sudo cat <<'EOF' | sudo tee chroot_dir/etc/X11/Xsession.d/95fcitx
          export GTK_IM_MODULE=fcitx
          export QT_IM_MODULE=fcitx
          export XMODIFIERS="@im=fcitx"
          EOF

          sudo chown -R 1000:1000 chroot_dir/home/duykhanh

      - name: Compress Filesystem into SquashFS
        run: |
          mkdir -p image/live
          sudo mksquashfs chroot_dir image/live/filesystem.squashfs -comp xz -processors $(nproc)

      - name: Extract Kernel and Setup GRUB
        run: |
          cp chroot_dir/boot/vmlinuz* image/live/vmlinuz
          cp chroot_dir/boot/initrd* image/live/initrd.img
          
          mkdir -p image/boot/grub
          cat <<'EOF' > image/boot/grub/grub.cfg
          set timeout=5
          set default=0
          
          menuentry "Boot DuyKhanhOS Live (Auto-Login Mode)" {
              linux /live/vmlinuz boot=casper quiet splash
              initrd /live/initrd.img
          }
          menuentry "Direct Install DuyKhanhOS to SSD" {
              linux /live/vmlinuz boot=casper calamares quiet splash
              initrd /live/initrd.img
          }
          EOF

      - name: Generate Custom Bootable ISO
        run: |
          grub-mkrescue -o duykhanhos-production.iso image

      - name: Upload Finished System ISO
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: duykhanh-os-production-iso
          path: duykhanhos-production.iso
