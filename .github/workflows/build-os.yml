name: Build DuyKhanhOS Live ISO

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-iso:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install ISO Generation Tools on Host
        run: |
          sudo apt-get update
          sudo apt-get install -y squashfs-tools xorriso grub-pc-bin mtools dosfstools rsync casper

      - name: Install All Graphic Apps Directly on Host Network
        run: |
          export DEBIAN_FRONTEND=noninteractive
          # Cài đặt trọn bộ giao diện đồ họa XFCE, cột sóng Wi-Fi, VLC và trình duyệt trực tiếp lên Host ngoài
          sudo apt-get install -y --no-install-recommends \
            xorg xfce4 xfce4-goodies lightdm plank \
            network-manager network-manager-gnome wpasupplicant \
            vlc libreoffice mousepad thunar gparted git firefox

      - name: Extract System Core via Docker and Fix Symlinks
        run: |
          if [ -d chroot_dir ]; then
            sudo umount -l chroot_dir/dev || true
            sudo umount -l chroot_dir/proc || true
            sudo umount -l chroot_dir/sys || true
          fi
          sudo rm -rf chroot_dir image duykhanhos-production.iso
          mkdir -p chroot_dir
          mkdir -p image/live

          # 1. Khởi tạo container từ Docker Hub sạch để lấy phôi hệ thống cơ bản
          docker create --name duykhanh_core ubuntu:noble
          mkdir -p docker_extract
          docker export duykhanh_core | tar -C docker_extract -xf -
          docker rm duykhanh_core

          # 2. BƠM FILE ĐỒ HỌA CHUẨN: Sử dụng rsync --copy-links biến toàn bộ Symlink ảo thành file thực tế,
          # Giúp nhân Linux của máy ảo VirtualBox nhận diện được driver màn hình và file thiết bị hiển thị /dev!
          # Đồng thời lọc bỏ các thư mục rác khổng lồ của GitHub để kiểm soát dung lượng không lo tràn đĩa
          for dir in bin sbin usr lib lib64 etc home dev; do
            if [ -d /docker_extract/$dir ]; then
              sudo rsync -a --copy-links /docker_extract/$dir/ chroot_dir/$dir/
            elif [ -d /$dir ]; then
              sudo rsync -a --copy-links /$dir/ chroot_dir/$dir/
            fi
          done

          # Dọn dẹp thư mục tạm và cache rác
          sudo rm -rf docker_extract
          sudo rm -rf chroot_dir/var/cache/apt/archives/*
          sudo rm -rf chroot_dir/var/lib/apt/lists/*

          # Tạo lại các phân vùng ảo trống bắt buộc cho Live ISO
          sudo mkdir -p chroot_dir/proc chroot_dir/sys chroot_dir/run chroot_dir/tmp chroot_dir/boot chroot_dir/var/log
          sudo chmod 1777 chroot_dir/tmp

      - name: Configure Auto-Login and User Account
        run: |
          # Thiết lập cấu hình tự động đăng nhập thẳng vào màn hình Desktop cho tài khoản duykhanh
          sudo mkdir -p chroot_dir/etc/lightdm/lightdm.conf.d
          sudo cat <<'EOF' | sudo tee chroot_dir/etc/lightdm/lightdm.conf.d/autologin.conf
          [Seat:*]
          autologin-user=duykhanh
          autologin-user-timeout=0
          user-session=xfce
          EOF

          # Khởi tạo tài khoản thực thi quyền quản trị sudo miễn mật khẩu
          sudo useradd -R chroot_dir -m -s /bin/bash -g sudo duykhanh || true
          sudo chroot chroot_dir passwd -d duykhanh || true

          sudo mkdir -p chroot_dir/etc/sudoers.d
          sudo cat <<'EOF' | sudo tee chroot_dir/etc/sudoers.d/g_sudo
          %sudo   ALL=(ALL:ALL) NOPASSWD: ALL
          EOF
          sudo chmod 0440 chroot_dir/etc/sudoers.d/g_sudo

      - name: Setup System AutoStart Configuration
        run: |
          sudo mkdir -p chroot_dir/home/duykhanh/.config/autostart
          
          # Tự khởi động thanh Dock kiểu Mac khi mở máy lên
          sudo cat <<'EOF' | sudo tee chroot_dir/home/duykhanh/.config/autostart/plank.desktop
          [Desktop Entry]
          Type=Application
          Exec=plank
          Name=Plank Dock
          X-GNOME-Autostart-enabled=true
          EOF

          sudo chown -R 1000:1000 chroot_dir/home/duykhanh

      - name: Compress System Core into SquashFS
        run: |
          # Tiến hành nén đa luồng hệ thống đồ họa sạch bằng thuật toán XZ
          sudo mksquashfs chroot_dir image/live/filesystem.squashfs -comp xz -processors $(nproc)

      - name: Extract Kernel and Setup GRUB Bootloader
        run: |
          sudo cp /boot/vmlinuz-$(uname -r) image/live/vmlinuz
          sudo cp /boot/initrd.img-$(uname -r) image/live/initrd.img
          
          mkdir -p image/boot/grub
          # ĐỒNG BỘ NHÃN ĐĨA VÀ KHÓA CHẶT ĐƯỜNG DẪN LIVE MEDIA PATH CHỐNG LỖI MÀN HÌNH ĐEN
          cat <<'EOF' > image/boot/grub/grub.cfg
          set timeout=10
          set default=0
          
          search --set=root --label DuyKhanhOS
          
          menuentry "1. Try DuyKhanhOS Live CD (Graphic Mode)" {
              linux /live/vmlinuz boot=casper live-media-path=/live quiet splash ---
              initrd /live/initrd.img
          }
          menuentry "2. Install DuyKhanhOS vĩnh viễn vào SSD" {
              linux /live/vmlinuz boot=casper live-media-path=/live toram quiet splash ---
              initrd /live/initrd.img
          }
          EOF

      - name: Generate Production Bootable ISO
        run: |
          sudo chmod -R 755 image
          
          # ĐÚC ĐĨA BOOT HOÀN HẢO: Sử dụng xorriso định danh chính xác nhãn đĩa cứng tên DuyKhanhOS
          xorriso -as mkisofs \
            -r -V "DuyKhanhOS" \
            -o duykhanhos-production.iso \
            -J -joliet-long \
            -b boot/grub/i386-pc/eltorito.img \
            -no-emul-boot -boot-load-size 4 -boot-info-table \
            image || grub-mkrescue -o duykhanhos-production.iso image

      - name: Upload Finished System ISO
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: duykhanh-os-production-iso
          path: duykhanhos-production.iso
